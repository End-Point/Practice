server:
  port: 8763

spring:
  application:
    #这个name很重要，通过这个名字调用该服务
    name: service-client

eureka:
  client:
    service-url:
     #这里是服务注册中心的地址，提供者通过这个地址向注册中心发送心跳
      defaultZone: http://localhost:8761/eureka/